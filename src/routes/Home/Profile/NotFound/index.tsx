import '../../../../index.css';

export default function NotFound() {

    return (
        < div className="profile-find-error container">
            <p>Erro ao buscar usuário</p>
        </div>
    );
}